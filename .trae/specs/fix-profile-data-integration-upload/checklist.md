# Checklist

- [x] 个人中心页面可正常加载，展示用户信息（收听时长、收藏数、歌单数等）
- [x] 前后端 API 返回格式统一为 `{ data: ..., total: ... }`
- [x] 所有 track 对象中 `id` 为 string 类型，`duration` 映射为 `dur` 字段
- [x] artists 列表每条包含 `id` 和 `name` 字段
- [x] albums 列表每条包含 `id` 字段
- [x] 后台管理目录挂载页面有上传区域，支持拖拽/点击上传
- [x] 上传的音频文件保存到 `music/` 目录，数据库自动创建记录
- [x] `music/` 目录下存在可播放的测试音频文件
- [x] 扫描器可发现 `music/` 目录下的文件并更新数据库
- [x] 播放器可播放 `music/` 目录下的真实音频文件